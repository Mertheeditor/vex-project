# Legacy Sprint Memory Location

A previous setup used `docs/sprint/`.

The canonical location is now:

`docs/agent-system/`

The legacy directory was not deleted. Compare and archive it only after the
agent-infrastructure checkpoint has been reviewed.
